'''
tagassess
=========

This module is divided in:
    `dao`: data access classes
    `data_parser`: functions to parse annotation files
    `entropy`: Entropy related functions
    `graph`: functions for creating tag graphs
    `index_creator`: Creates in memory indexes
    `probability_estimates`: Probability estimators based on likelihood.
    `smooth`: Smoothing functions
    `value_calculator`: Assess the value of tags based on mutual information
'''

